{
  "name": "Text Editors Dev Container",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y nano vim neovim && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*"
}
